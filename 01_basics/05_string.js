const name = "avi"
const count = 30
const gameName = new String("CALL OF DUTY");
// this is string interpolation , by this way we can create string in js
console.log(`hello my name is ${name} and my repo count is ${count}`);

